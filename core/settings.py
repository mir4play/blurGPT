"""Persistent user settings and built-in processing profiles."""

from __future__ import annotations

import json
from typing import Any

import config
from core.paths import SETTINGS_DIR if False else ROOT_DIR
